#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"

command -v ruby >/dev/null
command -v bundle >/dev/null
command -v chromium >/dev/null
command -v chromedriver >/dev/null
bundle check >/dev/null

echo "MailCatcher orb is ready"
